<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE MudletPackage>
<MudletPackage version="1.001">
	<ScriptPackage>
		<ScriptGroup isActive="yes" isFolder="yes">
			<name>pasek_kalendarz</name>
			<packageName>pasek_kalendarz</packageName>
			<script></script>
			<eventHandlerList />
			<Script isActive="yes" isFolder="no">
				<name>silnik</name>
				<packageName></packageName>
				<script>-- ============================================================
-- Pasek kalendarza dla Arkadii (Ishtar / Imperium) v1.4
-- Silnik kalendarza: port 1:1 z pluginow Dargoth
-- (ishtar_cal 1.8.11 / imperium_cal 1.8.12, autor: Isithunzi000)
-- Mechaniki "zycia" zegara: port z clock.ts klienta Delwing/Dargoth
-- (kotwice per domena, domena z GMCP, rekalibracja na wschod/zachod,
--  model precyzji, reczne ustawianie czasu)
-- ============================================================

pasek_kalendarz = pasek_kalendarz or {}
local pk = pasek_kalendarz
local PLUGIN_VERSION = "1.6.12"
local PLUGIN_BUILD   = "27-08-2026"

-- ---------- Wyjscie (kolory cecho, 1:1 z ishtar/imperium) ----------
local C_HEADER = "&lt;yellow&gt;"
local C_STAR   = "&lt;cyan&gt;"
local C_NOW    = "&lt;green&gt;"
local C_DATA   = "&lt;white&gt;"
local C_SEP    = "&lt;cyan&gt;"
local C_RESET  = "&lt;reset&gt;"

local function colorForLine(l)
  if l == "" then return C_RESET end
  if l:find("TRWA TERAZ", 1, true) or l:find("+++", 1, true) then return C_NOW end
  if l:find("***", 1, true) then return C_STAR end
  if l:sub(1, 1) ~= " " and l:sub(-1) == ":" then return C_HEADER end
  return C_DATA
end

local function safeCecho(s)
  if cecho then cecho(s) else io.write((s:gsub("&lt;%a+&gt;", ""))) end
end

-- wzorzec: onHelp z ishtar/imperium - linia 1 (tytul) zawsze C_HEADER
local function printTitledReport(lines, leadIn)
  safeCecho(leadIn .. C_SEP .. "---" .. C_RESET .. "\n")
  for i, l in ipairs(lines) do
    local col = (i == 1) and C_HEADER or colorForLine(l)
    safeCecho(col .. l .. C_RESET .. "\n")
  end
  safeCecho(C_SEP .. "---" .. C_RESET .. "\n\n")
end

-- globalne: alias reset to osobny chunk, lokalne nie przechodza miedzy chunkami
function pk.say(msg)
  safeCecho("\n" .. C_HEADER .. "[pasek_kalendarz] " .. msg .. C_RESET .. "\n")
end

-- ---------- KONFIGURACJA ----------
pk.cfg = pk.cfg or {
  x = 150, y = 20,
  width = 605, height = 58,       -- 605 px: najdluzszy tekst (59 zn. mono ~569 px) + marginesy
  ciemno_width = 115, ciemno_height = 24, ciemno_odstep = 4,  -- pigulka CIEMNO pod paskiem
  fontSize = 12,
  kolor_tla = {25, 20, 30, 210},     -- tlo paska (RGBA)
  kolor_tekstu = {255, 255, 255},    -- tekst paska (RGB)
  kolor_ciemno = {60, 20, 20, 230},  -- tlo pigulki CIEMNO (RGBA)
  pokazuj_precyzje = true,           -- "HH:MM ±N" gdy zegar jest czesciowo pewny (1-59)
  wlaczony = true,                   -- /pasek on|off (pokaz/schowaj pasek)
  window = false,                    -- /pasek window [on|off] (pasek w osobnym oknie)
}

-- wartosci fabryczne (dla komendy "/pasek domyslne")
pk.FABRYCZNE = {
  x = 150, y = 20,
  kolor_tla = {25, 20, 30, 210},
  kolor_tekstu = {255, 255, 255},
  kolor_ciemno = {60, 20, 20, 230},
}

-- ---------- STALE CZASU ----------
-- 1 minuta gry = 2 s realne (1 h gry = 120 s RL, doba gry = 48 min RL)
pk.SEKUND_RL_NA_MINUTE_GRY = 2
pk.SEKUND_RL_NA_GODZINE_GRY = 120
pk.MINUT_W_DOCIE = 24 * 60

-- ---------- ISHTAR (rachuba czasu Starszego Ludu) ----------
-- Rok zaczyna sie od 1 Saovine; 8 por roku po 45 dni = 360 dni.
pk.ISHTAR_PORY = {"Saovine", "Yule", "Imbaelk", "Birke", "Blathe", "Feainn", "Lammas", "Velen"}
pk.ISHTAR_DNI_W_PORZE = 45
pk.ISHTAR_DNI_W_ROKU = 360
pk.ISHTAR_WSCHOD = {6, 8, 7, 6, 5, 4, 5, 7}
pk.ISHTAR_ZACHOD = {17, 16, 18, 19, 21, 20, 20, 18}

-- Swieta o stalym dniu roku (linie "czas" bez frazy "dzien pory ..."),
-- np. "noc Midaete", "Belleteyn - Dzien Rozkwitu". Kazde wypada w ostatni
-- dzien poprzedzajacej pory roku (numeracja od 1 Saovine).
pk.ISHTAR_SWIETA = {
  midinvaerne = 45, birke = 135, midaete = 225, velen = 315,
  imbaelk = 90, belleteyn = 180, lammas = 270, saovine = 360,
}

-- ---------- IMPERIUM (Kalendarz Imperialny) ----------
-- 17 pozycji: 12 miesiecy (32/33 dni) + 5 swiat interkalarnych (1 dzien) = 400 dni.
-- {nazwa, dni, prefix (dni przed), wschod, zachod}
pk.IMP_MIESIACE = {
  {"Hexenstag",   1,   0, 8, 17},
  {"Nachexen",   32,   1, 8, 17},
  {"Jahrdrung",  33,  33, 7, 18},
  {"Mitterfruhl", 1,  66, 7, 18},
  {"Pflugzeit",  33,  67, 6, 19},
  {"Sigmarzeit", 33, 100, 5, 20},
  {"Sommerzeit", 33, 133, 5, 21},
  {"Sonnstill",   1, 166, 5, 22},
  {"Vorgeheim",  33, 167, 4, 22},
  {"Nachgeheim", 33, 200, 5, 21},
  {"Erntezeit",  33, 233, 5, 20},
  {"Mittherbst",  1, 266, 5, 20},
  {"Brauzeit",   33, 267, 6, 19},
  {"Kaldezeit",  33, 300, 6, 18},
  {"Ulriczeit",  33, 333, 7, 17},
  {"Mondstille",  1, 366, 8, 16},
  {"Vorhexen",   33, 367, 8, 16},
}
pk.IMP_DNI_W_ROKU = 400

-- Warianty pisowni z gry -&gt; nazwa kanoniczna
pk.IMP_ALIAS = {
  nachhexen = "nachexen", sigmarszeit = "sigmarzeit", kaltezeit = "kaldezeit",
  ulrichszeit = "ulriczeit", sonnenstill = "sonnstill", sonnenstil = "sonnstill",
  sonnenstille = "sonnstill", mondstill = "mondstille", mitterherbst = "mittherbst",
  hexensnacht = "hexenstag",
}

-- ---------- SLOWNIKI LICZEBNIKOW ----------
pk.HOUR_STEMS = {
  {"polnoc", 0}, {"pierwsz", 1}, {"drug", 2}, {"trzec", 3}, {"czwart", 4},
  {"piat", 5}, {"szost", 6}, {"siodm", 7}, {"osm", 8}, {"dziewiat", 9},
  {"dziesiat", 10}, {"jedenast", 11}, {"dwunast", 12}, {"poludnie", 12},
}

-- kolejnosc ma znaczenie (dziesiatki i nastki przed jednostkami)
pk.DAY_STEMS = {
  {"czterdziest", 40}, {"trzydziest", 30}, {"dwudziest", 20},
  {"dziewietnast", 19}, {"osiemnast", 18}, {"siedemnast", 17}, {"szesnast", 16},
  {"pietnast", 15}, {"czternast", 14}, {"trzynast", 13}, {"dwunast", 12},
  {"jedenast", 11}, {"dziesiat", 10}, {"dziewiat", 9}, {"osm", 8}, {"siodm", 7},
  {"szost", 6}, {"piat", 5}, {"czwart", 4}, {"trzec", 3}, {"drug", 2}, {"pierwsz", 1},
}

-- ---------- STAN ----------
pk.stan = pk.stan or { daylight = nil, season = nil, ciemno = false, domena = nil }
-- osobna kotwica dla kazdej domeny (jak w clock.ts): {doy, hour, minute, precision, ts}
pk.kotwice = pk.kotwice or { ishtar = nil, imperium = nil }
pk.pending_transition = pk.pending_transition or nil  -- {typ="wschod"/"zachod", ts}

-- ---------- POMOCNICZE ----------
local function lower(s) return string.lower(s or "") end

local function stem_value(word, stems)
  for _, pair in ipairs(stems) do
    local stem, val = pair[1], pair[2]
    if word:sub(1, #stem) == stem then return val end
  end
  return nil
end

function pk.dlugosc_roku(domena)
  return (domena == "imperium") and pk.IMP_DNI_W_ROKU or pk.ISHTAR_DNI_W_ROKU
end

-- "trzeci" -&gt; 3, "dwudziesty drugi" -&gt; 22, "17" -&gt; 17
function pk.parse_ordinal(text)
  text = lower(text):gsub("^%s+", ""):gsub("%s+$", "")
  if text == "" then return nil end
  local n = tonumber(text)
  if n then return math.floor(n) end
  local words = {}
  for w in text:gmatch("[%a]+") do words[#words + 1] = w end
  if #words == 1 then
    return stem_value(words[1], pk.DAY_STEMS)
  elseif #words == 2 then
    local t = stem_value(words[1], pk.DAY_STEMS)
    local u = stem_value(words[2], pk.DAY_STEMS)
    if t and u and t &gt;= 20 and u &lt; 10 then return t + u end
  end
  return nil
end

-- Godzina z linii "czas": slowo + regula PM (poludnie/wieczor/noca od 6).
function pk.parse_hour(line)
  local l = lower(line)
  local hw = l:match("^jest w przyblizeniu (%a+)")
  if not hw then return nil end
  local value = stem_value(hw, pk.HOUR_STEMS)
  if not value then return nil end
  local seg = l:match("^jest w przyblizeniu %a+%s*(.-)%s*,") or ""
  local daytime
  for _, w in ipairs({"poludniu", "poludniem", "poludnie", "wieczorem", "nocy", "ranem", "rano", "dzien"}) do
    if seg:find(w, 1, true) then daytime = w break end
  end
  local hours = value
  -- PM tylko dla "poludniu"/"wieczorem"/"nocy od 6" (paritet z ishtar_cal):
  -- "poludnie"/"poludniem" po slowie godziny NIE dodaja 12 (inaczej "poludnie poludniem" dawalo 0).
  if daytime == "poludniu" or daytime == "wieczorem" or (daytime == "nocy" and value &gt;= 6) then
    hours = hours + 12
  end
  if hours &gt; 23 then hours = 0 end
  return hours
end

function pk.wykryj_domene(line)
  local l = lower(line)
  if l:find("rachuby czasu starszego ludu", 1, true) then return "ishtar" end
  if l:find("kalendarza imperialnego", 1, true) then return "imperium" end
  return nil
end

function pk.pora_index(pora)
  local p = lower(pora)
  for i, nazwa in ipairs(pk.ISHTAR_PORY) do
    if lower(nazwa) == p then return i end
  end
  return nil
end

function pk.miesiac_index(miesiac)
  local m = lower(miesiac):gsub("[^%a]", "")
  m = pk.IMP_ALIAS[m] or m
  for i, row in ipairs(pk.IMP_MIESIACE) do
    if lower(row[1]) == m then return i end
  end
  return nil
end

-- ---------- PARSER: ISHTAR ----------
function pk.parse_ishtar(line)
  local hour = pk.parse_hour(line)
  if not hour then return nil end
  local l = lower(line)

  -- wariant dwuslowny: "dwudziesty drugi dzien pory Saovine"
  local d1, d2, pora = l:match("(%a+)%s+(%a+)%s+dzien%s+pory%s+(%a+)")
  local day
  if pora then
    local t = stem_value(d1, pk.DAY_STEMS)
    local u = stem_value(d2, pk.DAY_STEMS)
    if t and u and t &gt;= 20 and u &lt; 10 then day = t + u end
  end
  -- wariant jednoslowny / cyfry: "trzeci dzien pory Yule" / "3 dzien pory Yule"
  if not day then
    local dt, p2 = l:match("([%a%d]+)%s+dzien%s+pory%s+(%a+)")
    if dt and p2 then
      day = pk.parse_ordinal(dt)
      pora = p2
    end
  end
  if day and pora then
    local pidx = pk.pora_index(pora)
    if pidx and day &gt;= 1 and day &lt;= pk.ISHTAR_DNI_W_PORZE then
      return { doy = (pidx - 1) * pk.ISHTAR_DNI_W_PORZE + day, hour = hour }
    end
  end

  -- dni specjalne bez frazy "dzien pory ..."
  for name, doy in pairs(pk.ISHTAR_SWIETA) do
    if l:find("%f[%a]" .. name .. "%f[^%a]") then
      return { doy = doy, hour = hour }
    end
  end
  return nil
end

-- ---------- PARSER: IMPERIUM ----------
function pk.parse_imperium(line)
  local hour = pk.parse_hour(line)
  if not hour then return nil end
  local l = lower(line)

  -- "&lt;dzien&gt; dzien miesiaca &lt;miesiac&gt;" (dzien slownie, zlozony lub cyfra)
  local d1, d2, mon = l:match("(%a+)%s+(%a+)%s+dzien%s+miesiaca%s+(%a+)")
  local day
  if mon then
    local t = stem_value(d1, pk.DAY_STEMS)
    local u = stem_value(d2, pk.DAY_STEMS)
    if t and u and t &gt;= 20 and u &lt; 10 then day = t + u end
    if not day then mon = nil end  -- falszywe trafienie, probujemy wariant jednoslowny
  end
  if not day then
    local dt, m2 = l:match("([%a%d]+)%s+dzien%s+miesiaca%s+(%a+)")
    if dt and m2 then
      day = pk.parse_ordinal(dt)
      mon = m2
    end
  end
  if day and mon then
    local midx = pk.miesiac_index(mon)
    if midx then
      local row = pk.IMP_MIESIACE[midx]
      if day &gt;= 1 and day &lt;= row[2] then
        return { doy = row[3] + day, hour = hour }
      end
    end
    return nil
  end

  -- swieta interkalarne: "(dzien|noc) &lt;Swieto&gt; wedlug ..." -&gt; 1. dzien pozycji
  local sw = l:match("dzien%s+(%a+)[%.,]?%s+wedlug") or l:match("noc%s+(%a+)[%.,]?%s+wedlug")
  if sw then
    local midx = pk.miesiac_index(sw)
    if midx then
      return { doy = pk.IMP_MIESIACE[midx][3] + 1, hour = hour }
    end
    -- np. "noc Geheimnisnacht": bez daty w klauzuli - NIE zgadujemy,
    -- zostaje ekstrapolacja ze starej kotwicy.
  end
  return nil
end

-- ---------- KOTWICE + EKSTRAPOLACJA ----------
function pk.sciezka_kotwicy()
  return getMudletHomeDir() .. "/pasek_kalendarz_kotwice.lua"
end

function pk.zapisz_kotwice()
  pcall(function()
    table.save(pk.sciezka_kotwicy(), { kotwice = pk.kotwice, cfg = {
      x = pk.cfg.x, y = pk.cfg.y,
      kolor_tla = pk.cfg.kolor_tla, kolor_tekstu = pk.cfg.kolor_tekstu,
      kolor_ciemno = pk.cfg.kolor_ciemno,
      wlaczony = pk.cfg.wlaczony,
      window = pk.cfg.window,
    } })
  end)
end

function pk.wczytaj_kotwice()
  local ok, wynik = pcall(function()
    local t = {}
    table.load(pk.sciezka_kotwicy(), t)
    return t
  end)
  if not ok or type(wynik) ~= "table" then return end
  if type(wynik.kotwice) == "table" then
    pk.kotwice = wynik.kotwice
  elseif type(wynik.kotwica) == "table" and wynik.kotwica.doy then
    -- migracja ze starego formatu (v1.0): pojedyncza kotwica
    local k = wynik.kotwica
    if k.domena then pk.kotwice[k.domena] = k end
  end
  -- zapisana konfiguracja wygladu/pozycji (komendy "/pasek ...")
  if type(wynik.cfg) == "table" then
    for _, klucz in ipairs({"x", "y", "kolor_tla", "kolor_tekstu", "kolor_ciemno", "wlaczony", "window"}) do
      if wynik.cfg[klucz] ~= nil then pk.cfg[klucz] = wynik.cfg[klucz] end
    end
  end
end

-- Aktualny czas gry dla domeny: ekstrapolacja z kotwicy (1 min gry = 2 s RL).
function pk.aktualny_czas(domena)
  domena = domena or pk.stan.domena
  if not domena then return nil end
  local k = pk.kotwice[domena]
  if not k then return nil end
  local minut_gry = math.floor((os.time() - k.ts) / pk.SEKUND_RL_NA_MINUTE_GRY)
  local total = ((k.doy - 1) * pk.MINUT_W_DOCIE + k.hour * 60 + (k.minute or 0) + minut_gry)
                % (pk.dlugosc_roku(domena) * pk.MINUT_W_DOCIE)
  local doy = math.floor(total / pk.MINUT_W_DOCIE) + 1
  local hh = math.floor((total % pk.MINUT_W_DOCIE) / 60)
  local mm = total % 60
  return domena, doy, hh, mm, (k.precision or 0)
end

-- ---------- KALENDARZ: doy -&gt; nazwa ----------
function pk.ishtar_pora_dnia(doy)
  local idx = math.floor((doy - 1) / pk.ISHTAR_DNI_W_PORZE) + 1
  return pk.ISHTAR_PORY[idx], ((doy - 1) % pk.ISHTAR_DNI_W_PORZE) + 1, idx
end

function pk.imp_miesiac_dnia(doy)
  for i, row in ipairs(pk.IMP_MIESIACE) do
    if doy &lt;= row[3] + row[2] then
      return row[1], doy - row[3], i
    end
  end
  return pk.IMP_MIESIACE[17][1], doy - pk.IMP_MIESIACE[17][3], 17
end

-- Wschod/zachod z tabeli dla danego dnia (do cross-checku i fallbacku daylight)
function pk.wschod_zachod(domena, doy)
  if domena == "imperium" then
    local _, _, mi = pk.imp_miesiac_dnia(doy)
    return pk.IMP_MIESIACE[mi][4], pk.IMP_MIESIACE[mi][5]
  end
  local _, _, pii = pk.ishtar_pora_dnia(doy)
  return pk.ISHTAR_WSCHOD[pii], pk.ISHTAR_ZACHOD[pii]
end

-- Dzien/noc z godziny (gdy GMCP nie dostarczylo daylight)
function pk.jest_jasno(domena, doy, hh)
  local wschod, zachod = pk.wschod_zachod(domena, doy)
  return hh &gt;= wschod and hh &lt; zachod
end

-- ---------- REKALIBRACJA NA WSCHODZIE/ZACHODZIE (port z clock.ts) ----------
-- Zmiana daylight z GMCP = zaobserwowany wschod/zachod. Zegar dokreca sie
-- do pelnej godziny (zaokraglenie w gore) z precyzja 0 - bez wysylania "czas".
function pk.rekalibruj_na_przejsciu(typ)
  local domena = pk.stan.domena
  if not domena then
    pk.pending_transition = { typ = typ, ts = os.time() }
    return
  end
  if not pk.kotwice[domena] then
    pk.pending_transition = { typ = typ, ts = os.time() }
    return
  end

  local _, doy, hh, mm = pk.aktualny_czas(domena)
  if mm &gt; 0 then hh = hh + 1 end
  if hh &gt; 23 then
    hh = 0
    doy = doy % pk.dlugosc_roku(domena) + 1
  end

  -- cross-check z tabela wschodow/zachodow (tylko log diagnostyczny)
  local wschod, zachod = pk.wschod_zachod(domena, doy)
  local oczekiwana = (typ == "wschod") and wschod or zachod
  if oczekiwana ~= hh and debugc then
    debugc(string.format("[pasek] %s: %s o %02d:00, tabela mowi %02d:00 (dzien %d)",
      domena, typ, hh, oczekiwana, doy))
  end

  pk.kotwice[domena] = { doy = doy, hour = hh, minute = 0, precision = 0, ts = os.time() }
  pk.zapisz_kotwice()
end

-- Oczekujaca tranzycja (gdy przyszla przed pierwsza kotwica) - wazna 1 h gry.
function pk.consume_pending()
  local p = pk.pending_transition
  pk.pending_transition = nil
  if not p then return nil end
  if os.time() - p.ts &gt; pk.SEKUND_RL_NA_GODZINE_GRY then return nil end
  return p.typ
end

-- ---------- OBSŁUGA LINII "czas" ----------
function pk.on_czas_line(line)
  local domena = pk.wykryj_domene(line)
  if domena then
    local parsed = (domena == "ishtar") and pk.parse_ishtar(line) or pk.parse_imperium(line)
    if parsed then
      local k = pk.kotwice[domena]
      local minute, precision
      local pending = pk.consume_pending()
      if pending then
        -- czas przyszedl tuz po wschodzie/zachodzie: zegar dokladny
        minute, precision = 0, 0
      elseif k then
        local _, cdoy, chh, cmm = pk.aktualny_czas(domena)
        if cdoy == parsed.doy and chh == parsed.hour then
          -- zgodnosc: zachowaj minuty, zwez precyzje do konca biezacej godziny
          minute = cmm
          precision = math.min(k.precision or 60, math.max(0, 60 - cmm))
        else
          -- rozjazd: rekalibracja na godzine z "czas"
          minute, precision = 0, 60
        end
      else
        minute, precision = 0, 60
      end
      pk.kotwice[domena] = { doy = parsed.doy, hour = parsed.hour, minute = minute,
                             precision = precision, ts = os.time() }
      pk.zapisz_kotwice()
      if not pk.stan.domena then pk.stan.domena = domena end
    end
    -- jak nie sparsowane (np. Geheimnisnacht): zostaje stara kotwica (nie zgadujemy)
    pk.render()
  end

end

-- ---------- RECZNE USTAWIENIE (jak "/czas imperium 8 273" w Dargoth) ----------
function pk.ustaw_recznie(domena_txt, godzina_txt, doy_txt)
  local domena = (lower(domena_txt) == "imperium") and "imperium" or "ishtar"
  local godzina = tonumber(godzina_txt)
  if not godzina or godzina &lt; 0 or godzina &gt; 23 then
    pk.say("Godzina musi byc w zakresie 0-23.")
    return
  end
  local _, doy_akt = pk.aktualny_czas(domena)
  local doy = tonumber(doy_txt) or doy_akt or 1
  if doy &lt; 1 or doy &gt; pk.dlugosc_roku(domena) then
    pk.say(string.format("Dzien roku musi byc w zakresie 1-%d.", pk.dlugosc_roku(domena)))
    return
  end
  pk.kotwice[domena] = { doy = doy, hour = godzina, minute = 0, precision = 0, ts = os.time() }
  pk.zapisz_kotwice()
  pk.stan.domena = domena
  pk.render()
  pk.say(string.format("Ustawiono czas %s: %02d:00, dzien %d.",
    domena == "imperium" and "Imperium" or "Ishtar", godzina, doy))
end

-- ---------- RESET KOTWIC CZASU -------------------------------------------
-- 1.6.11: reset NIE usuwa pliku kotwic - plik trzyma tez cfg (pozycja,
-- kolory, wlaczony, window), wiec kotwice czyscimy w RAM i nadpisujemy
-- plik zapisem; zapisany wyglad zostaje (reset wygladu: /pasek domyslne).
-- Bez argumentu czysci obie domeny; z domena - tylko wskazana.
function pk.komenda_reset(arg)
  if arg == nil then
    pk.kotwice = { ishtar = nil, imperium = nil }
    pk.zapisz_kotwice()
    pk.render()
    pk.say("Kotwice czasu wyczyszczone dla obu domen. Wpisz czas, zeby zsynchronizowac.")
  elseif arg == "ishtar" or arg == "imperium" then
    pk.kotwice[arg] = nil
    pk.zapisz_kotwice()
    pk.render()
    local d = (arg == "imperium") and "Imperium" or "Ishtar"
    local druga = (arg == "imperium") and "Ishtar" or "Imperium"
    pk.say(string.format(
      "Kotwice czasu wyczyszczone dla domeny %s (%s bez zmian). Wpisz czas, zeby zsynchronizowac.",
      d, druga))
  else
    pk.say("Uzycie: /pasek reset [ishtar|imperium]")
  end
end

-- ---------- KOMENDY WYGLADU/POZYCJI (zapisywane na dysk) ----------
local function tohex(k) return string.format("#%02x%02x%02x", k[1], k[2], k[3]) end

-- Aplikuje cfg na istniejace labele (pozycja + kolory + odswiezenie)
function pk.zastosuj_wyglad()
  -- baza pozycji: inline z cfg; w trybie okna etykiety wzgledem okna (0,0)
  -- (kontener okna istnieje tez po wyjsciu z trybu - liczy sie flaga)
  local bx, by = pk.cfg.x, pk.cfg.y
  if pk.cfg.window == true and pk.win_container then bx, by = 0, 0 end
  if pk.label then
    pk.label:setColor(unpack(pk.cfg.kolor_tla))
    pk.label.fgColor = tohex(pk.cfg.kolor_tekstu)
    setFgColor(pk.label.name, unpack(pk.cfg.kolor_tekstu))
    pk.label:move(bx, by)
    -- widocznosc z cfg (/pasek on|off): egzekwowana przy kazdej zmianie wygladu
    if pk.cfg.wlaczony == false then pk.label:hide() else pk.label:show() end
  end
  if pk.ciemno_label then
    pk.ciemno_label:setColor(unpack(pk.cfg.kolor_ciemno))
    pk.ciemno_label:move(bx + math.floor((pk.cfg.width - pk.cfg.ciemno_width) / 2),
                         by + pk.cfg.height + pk.cfg.ciemno_odstep)
  end
  -- osobne okno podaza za widocznoscia paska (/pasek on|off), tylko
  -- gdy tryb okna jest aktywny (po window off okno zostaje zamkniete)
  if pk.cfg.window == true and pk.win_container then
    if pk.cfg.wlaczony == false then pk.win_container:hide() else pk.win_container:show() end
  end
  pk.render()
end

local function parse_rgb(rs, gs, bs, as, domyslne_a)
  local r, g, b, a = tonumber(rs), tonumber(gs), tonumber(bs), tonumber(as)
  if not (r and g and b) then return nil end
  a = a or domyslne_a
  for _, v in ipairs({r, g, b, a}) do
    if v &lt; 0 or v &gt; 255 then return nil end
  end
  return {r, g, b, a}
end

function pk.komenda_pozycja(xs, ys)
  local x, y = tonumber(xs), tonumber(ys)
  if not x or not y then pk.say("Uzycie: /pasek pozycja X Y") return end
  pk.cfg.x, pk.cfg.y = math.floor(x), math.floor(y)
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say(string.format("Pozycja ustawiona: %d, %d.", pk.cfg.x, pk.cfg.y))
end

function pk.komenda_on()
  pk.cfg.wlaczony = true
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say("Pasek wlaczony.")
end

function pk.komenda_off()
  pk.cfg.wlaczony = false
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say("Pasek wylaczony.")
end

-- ---------- FLOAT: przenoszenie paska mysza (sesyjne, auto-blokada 60 s) ----------
pk.float_on = false       -- tryb przenoszenia (celowo nie persystowany)
pk.float_drag = nil       -- aktywny chwyt { dx, dy, moved }
pk.float_timer = nil      -- id tempTimer auto-blokady

local FLOAT_AUTOLOCK_S = 60

-- Clamp pozycji do glownego okna (pasek + pigulka CIEMNO musza sie zmiescic).
function pk.clamp_xy(x, y)
  local winW, winH = 1024, 768
  if type(getMainWindowSize) == "function" then
    local w, h = getMainWindowSize()
    winW, winH = tonumber(w) or winW, tonumber(h) or winH
  end
  local maxX = math.max(0, winW - pk.cfg.width)
  local maxY = math.max(0, winH - pk.cfg.height - pk.cfg.ciemno_odstep - pk.cfg.ciemno_height)
  return math.max(0, math.min(x, maxX)), math.max(0, math.min(y, maxY))
end

-- Auto-blokada: 60 s od ostatniego przeciagniecia; timer wypadniety w trakcie
-- draga odklada sie do release (re-arm), nigdy nie odcina w locie.
function pk.float_arm()
  if pk.float_timer then pcall(killTimer, pk.float_timer) end
  pk.float_timer = tempTimer(FLOAT_AUTOLOCK_S, function()
    if pk.float_drag then
      pk.float_arm()
    else
      pk.float_on = false
      pk.float_timer = nil
      pk.say("Przenoszenie wygaslo (60 s bez przenoszenia) - pasek zablokowany.")
    end
  end)
end

local function float_move_pill()
  if pk.ciemno_label then
    pk.ciemno_label:move(pk.cfg.x + math.floor((pk.cfg.width - pk.cfg.ciemno_width) / 2),
                         pk.cfg.y + pk.cfg.height + pk.cfg.ciemno_odstep)
  end
end

-- Klik poza trybem float = synchronizacja (jak dotad); w trybie float klik
-- rozpoczyna chwyt, a synchronizacja odpala sie dopiero na czystym release.
function pk.float_on_click(ev)
  -- guard obronny (1.6.11): w trybie okna zawsze zwykly klik (inwarjant:
  -- float_on nigdy nie powinien byc true w oknie)
  if not pk.float_on or pk.cfg.window == true then send("czas") return end
  ev = ev or {}
  pk.float_drag = { dx = tonumber(ev.x) or 0, dy = tonumber(ev.y) or 0, moved = false }
end

function pk.float_on_move(ev)
  if pk.cfg.window == true then return end  -- guard obronny (1.6.11): brak dragowania w oknie
  local d = pk.float_drag
  if not (pk.float_on and d and pk.label) then return end
  ev = ev or {}
  local ex, ey = tonumber(ev.x) or d.dx, tonumber(ev.y) or d.dy
  local nx, ny = pk.clamp_xy(pk.cfg.x + (ex - d.dx), pk.cfg.y + (ey - d.dy))
  if nx ~= pk.cfg.x or ny ~= pk.cfg.y then
    if not d.moved and math.abs(ex - d.dx) + math.abs(ey - d.dy) &gt; 1 then
      d.moved = true
    end
    pk.cfg.x, pk.cfg.y = nx, ny
    pk.label:move(nx, ny)
    float_move_pill()
  end
end

function pk.float_on_release()
  if pk.cfg.window == true then pk.float_drag = nil return end  -- guard obronny (1.6.11)
  local d = pk.float_drag
  pk.float_drag = nil
  if not (pk.float_on and d) then return end
  if d.moved then
    pk.zapisz_kotwice()
    pk.float_arm()
  else
    send("czas")
  end
end

function pk.komenda_float(arg)
  if arg == nil or arg == "on" then
    -- guard: uzbrojenie timera wymaga widocznego paska; wyjatkiem jest
    -- samo /pasek float w trybie okna (tylko zmiana umiejscowienia)
    if pk.cfg.wlaczony == false and not (arg == nil and pk.cfg.window == true) then
      pk.say("Pasek jest wylaczony - najpierw /pasek on.")
      return
    end
    -- przejscie z trybu okna: samo float = sam powrot do glownego okna,
    -- float on = powrot + przenoszenie z timerem (1.6.11)
    if pk.cfg.window == true then
      pk.cfg.window = false
      pk.zapisz_kotwice()
      pk.window_wyjdz()
      if arg == nil then
        pk.say("Pasek wrocil do glownego okna.")
        return
      end
      pk.float_on = true
      pk.float_arm()
      if pk.label and pk.label.raise then pk.label:raise() end
      pk.say("Pasek wrocil do glownego okna - przenoszenie mysza wlaczone na 60 s.")
      return
    end
    pk.float_on = true
    pk.float_arm()
    if pk.label and pk.label.raise then pk.label:raise() end
    pk.say("Przenoszenie mysza wlaczone na 60 s - przeciagnij pasek.")
  elseif arg == "off" then
    if pk.float_on then
      pk.float_on = false
      pk.float_drag = nil
      if pk.float_timer then pcall(killTimer, pk.float_timer) pk.float_timer = nil end
      pk.say("Przenoszenie mysza wylaczone.")
    else
      pk.say("Przenoszenie mysza juz jest wylaczone.")
    end
  else
    pk.say("Uzycie: /pasek float [on|off]")
  end
end

-- ---------- WINDOW: pasek w osobnym plywajacym oknie (userwindow) ----------
pk.window_name = "pasek_kalendarz_okno"
pk.win_container = nil    -- Geyser.UserWindow, gdy aktywny tryb okna
pk.win_last_h = nil       -- ostatnia zaaplikowana wysokosc okna (resize tylko przy zmianie)
-- 1.6.12: resizeWindow ustawia rozmiar ZEWNETRZNY okna (z paskiem tytulu
-- i obwodkami) - bez zapasu obszar roboczy ucinal druga linie tekstu.
-- Zapas na dekoracje: zmierzone 17 px (Linux), Windows/macOS wiecej;
-- tekst konczy sie ~y52, wiec bezpieczny dla dekoracji do 30 px.
pk.WINDOW_PAD_X = 4    -- obwodki boczne (zmierzone 2 px + margines)
pk.WINDOW_PAD_Y = 26   -- pasek tytulu + obwodki
-- 1.6.11: mechanizm float_suspended usuniety - float_on i cfg.window sa
-- wzajemnie wykluczajacymi sie stanami (inwarjant pilnowany w komendach)

-- userwindow wymaga wsparcia klienta (mudlet-web go nie ma); setWindow
-- (przepiecie etykiet) jest dostepne od Mudlet 4.8; resizeWindow obsluguje
-- tez userwindow (Host::resizeWindow, galaz dockwidget)
function pk.window_dostepne()
  return type(openUserWindow) == "function"
     and type(resizeWindow) == "function"
     and type(setWindow) == "function"
     and type(Geyser) == "table" and type(Geyser.UserWindow) == "table"
     and not (type(mudletOlderThan) == "function" and mudletOlderThan(4, 8))
end

-- wysokosc tresci okna: sam pasek, a przy widocznym CIEMNO + odstep i pigulka
function pk.window_wysokosc()
  local h = pk.cfg.height
  if pk.stan.ciemno then h = h + pk.cfg.ciemno_odstep + pk.cfg.ciemno_height end
  return h
end

-- rozmiar zewnetrzny okna (1.6.12): tresc + zapas na dekoracje systemowe
function pk.window_rozmiar()
  return pk.cfg.width + pk.WINDOW_PAD_X, pk.window_wysokosc() + pk.WINDOW_PAD_Y
end

-- Dynamiczny resize okna przy zmianie stanu CIEMNO (bez spamu co tick).
-- Label paska jest NADWYMIAROWY (= rozmiar zewnetrzny okna): klient utnie
-- go do obszaru roboczego, wiec tlo zawsze wypelnia okno do krawedzi.
function pk.window_resize()
  if not (pk.cfg.window == true and pk.win_container) then return end
  local w, h = pk.window_rozmiar()
  if pk.win_last_h ~= h then
    resizeWindow(pk.window_name, w, h)
    if pk.label then pk.label:resize(w, h) end
    pk.win_last_h = h
  end
end

-- Wejscie w tryb okna: userwindow tworzone raz (autoDock wylaczone,
-- restoreLayout - rozmiar zawsze wymuszany jawnie), etykiety przepinane
-- przez setWindow bez odtwarzania.
function pk.window_wejdz()
  local w, h = pk.window_rozmiar()
  if not pk.win_container then
    pk.win_container = Geyser.UserWindow:new({
      name = pk.window_name,
      x = 0, y = 0,
      width = w, height = h,
      autoDock = false,
      restoreLayout = true,
      titleText = "Pasek kalendarza - Arkadia",
    })
  end
  -- domyslny rozmiar wymuszany przy kazdym wejsciu w tryb okna
  -- (resizeWindow od-dokowuje siłowo okno, gdyby bylo zadokowane);
  -- label nadwymiarowy = wypelnienie calego obszaru roboczego (1.6.12)
  resizeWindow(pk.window_name, w, h)
  pk.win_last_h = h
  -- przepiecie etykiet do okna (pozycje wzgledem okna)
  setWindow(pk.window_name, "pasek_kalendarz_label", 0, 0, true)
  pk.label:resize(w, h)
  setWindow(pk.window_name, "pasek_kalendarz_ciemno",
            math.floor((pk.cfg.width - pk.cfg.ciemno_width) / 2),
            pk.cfg.height + pk.cfg.ciemno_odstep, pk.stan.ciemno == true)
  pk.zastosuj_wyglad()
end

-- Powrot inline: etykiety z powrotem do glownego okna na pozycje z cfg,
-- osobne okno zamkniete (kontener zostaje do ponownego uzycia).
function pk.window_wyjdz()
  -- label wraca do fabrycznych wymiarow inline (w oknie byl nadwymiarowy)
  if pk.label then pk.label:resize(pk.cfg.width, pk.cfg.height) end
  setWindow("main", "pasek_kalendarz_label", pk.cfg.x, pk.cfg.y, pk.cfg.wlaczony ~= false)
  setWindow("main", "pasek_kalendarz_ciemno",
            pk.cfg.x + math.floor((pk.cfg.width - pk.cfg.ciemno_width) / 2),
            pk.cfg.y + pk.cfg.height + pk.cfg.ciemno_odstep,
            pk.stan.ciemno == true and pk.cfg.wlaczony ~= false)
  if type(closeUserWindow) == "function" then closeUserWindow(pk.window_name) end
  pk.zastosuj_wyglad()
end

-- Buduje labele paska: inline w glownym oknie albo w osobnym userwindow.
function pk.buduj_labele()
  if pk.label then pk.label:hide() end
  if pk.ciemno_label then pk.ciemno_label:hide() end
  -- etykiety zawsze inline w glownym oknie; tryb okna przepina je przez
  -- setWindow (globalny rejestr nazw labeli odrzuca odtworzenie w userwindow)
  local bx, by = pk.cfg.x, pk.cfg.y
  pk.label = Geyser.Label:new({
    name = "pasek_kalendarz_label",
    x = bx, y = by,
    width = pk.cfg.width, height = pk.cfg.height,
    fgColor = "white",
    message = "",
  })
  pk.label:setColor(unpack(pk.cfg.kolor_tla))
  pk.label:setFontSize(pk.cfg.fontSize)
  pk.label:setClickCallback(function(ev) pk.float_on_click(ev) end)
  pk.label:setMoveCallback(function(ev) pk.float_on_move(ev) end)
  pk.label:setReleaseCallback(function(ev) pk.float_on_release(ev) end)

  -- pigulka CIEMNO: osobny label pod paskiem, wysrodkowany wzgledem niego
  pk.ciemno_label = Geyser.Label:new({
    name = "pasek_kalendarz_ciemno",
    x = bx + math.floor((pk.cfg.width - pk.cfg.ciemno_width) / 2),
    y = by + pk.cfg.height + pk.cfg.ciemno_odstep,
    width = pk.cfg.ciemno_width, height = pk.cfg.ciemno_height,
    fgColor = "white",
    message = "&lt;center&gt;◆ CIEMNO",
  })
  pk.ciemno_label:setColor(unpack(pk.cfg.kolor_ciemno))
  pk.ciemno_label:setFontSize(math.max(8, pk.cfg.fontSize - 2))
  pk.ciemno_label:hide()

  -- zastosuj zapisany wyglad (pozycja/kolory tekstu) i odswiez
  pk.zastosuj_wyglad()
end

function pk.komenda_window(arg)
  -- samo /pasek window = przelacznik: glowne okno &lt;-> osobne okno (1.6.11)
  if arg == nil then
    arg = (pk.cfg.window == true) and "off" or "on"
  end
  if arg == "on" then
    if pk.cfg.window == true then
      pk.say("Pasek juz jest w osobnym oknie.")
      return
    end
    if not pk.window_dostepne() then
      pk.say("Tryb osobnego okna niedostepny w tym kliencie.")
      return
    end
    -- wejscie w okno gasi przenoszenie na stale (bez usypiania - 1.6.11);
    -- przenoszenie wlacza sie tylko swiadoma komenda w glownym oknie
    if pk.float_on then
      pk.float_on = false
      pk.float_drag = nil
      if pk.float_timer then pcall(killTimer, pk.float_timer) pk.float_timer = nil end
    end
    pk.cfg.window = true
    pk.zapisz_kotwice()
    pk.window_wejdz()
    pk.say("Pasek przeniesiony do osobnego okna (powrot: /pasek window off).")
  elseif arg == "off" then
    if pk.cfg.window ~= true then
      pk.say("Pasek juz jest w glownym oknie.")
      return
    end
    pk.cfg.window = false
    pk.zapisz_kotwice()
    pk.window_wyjdz()
    pk.say("Pasek wrocil do glownego okna.")
  else
    pk.say("Uzycie: /pasek window [on|off]")
  end
end

function pk.komenda_tlo(rs, gs, bs, as)
  local k = parse_rgb(rs, gs, bs, as, 210)
  if not k then pk.say("Uzycie: /pasek tlo R G B [A], wartosci 0-255") return end
  pk.cfg.kolor_tla = k
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say(string.format("Tlo paska: %d %d %d %d.", k[1], k[2], k[3], k[4]))
end

function pk.komenda_tekst(rs, gs, bs)
  local k = parse_rgb(rs, gs, bs, nil, 255)
  if not k then pk.say("Uzycie: /pasek tekst R G B, wartosci 0-255") return end
  pk.cfg.kolor_tekstu = {k[1], k[2], k[3]}
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say(string.format("Kolor tekstu: %d %d %d.", k[1], k[2], k[3]))
end

function pk.komenda_ciemno(rs, gs, bs, as)
  local k = parse_rgb(rs, gs, bs, as, 230)
  if not k then pk.say("Uzycie: /pasek ciemno R G B [A], wartosci 0-255") return end
  pk.cfg.kolor_ciemno = k
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say(string.format("Tlo CIEMNO: %d %d %d %d.", k[1], k[2], k[3], k[4]))
end

function pk.komenda_domyslne()
  pk.cfg.x = pk.FABRYCZNE.x
  pk.cfg.y = pk.FABRYCZNE.y
  pk.cfg.kolor_tla = {unpack(pk.FABRYCZNE.kolor_tla)}
  pk.cfg.kolor_tekstu = {unpack(pk.FABRYCZNE.kolor_tekstu)}
  pk.cfg.kolor_ciemno = {unpack(pk.FABRYCZNE.kolor_ciemno)}
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
  pk.say("Przywrocono ustawienia fabryczne.")
end

-- ---------- PALETA KOLOROW: gole /pasek tlo i /pasek ciemno ----------
-- Panel: 24 probki (8x3), suwak alfa, podglad, X. Widgety budowane leniwie
-- RAZ (rejestr nazw labeli odrzuca duplikaty), potem tylko show/hide.
-- Panel zawsze w glownym oknie; aplikacja cicha (feedback wizualny).
pk.PALETA_KOLORY = {
  {0x00,0x00,0x00}, {0x1a,0x1a,0x1a}, {0x33,0x33,0x33}, {0x66,0x66,0x66},
  {0x99,0x99,0x99}, {0xcc,0xcc,0xcc}, {0xe6,0xe6,0xe6}, {0xff,0xff,0xff},
  {0xcc,0x00,0x00}, {0xe6,0x73,0x00}, {0xe6,0xb8,0x00}, {0x2e,0x8b,0x22},
  {0x00,0x80,0x6b}, {0x00,0x59,0xb3}, {0x40,0x30,0xc0}, {0x8b,0x1a,0x8f},
  {0x4d,0x1a,0x1a}, {0x4d,0x33,0x19}, {0x4d,0x4d,0x1a}, {0x1a,0x33,0x19},
  {0x1a,0x33,0x33}, {0x19,0x14,0x1e}, {0x26,0x19,0x33}, {0x33,0x1a,0x26},
}
pk.PALETA_SZER = 292   -- 8*32 + 7*4 + 2*4
pk.PALETA_WYS = 196
pk.PALETA_SUWAK_W = 284
pk.paleta = { cel = nil, otwarta = false, alfa = 255 }

-- wzgledne pozycje widgetow panelu (dx, dy, w, h) wzgledem tla panelu
local function paleta_layout()
  local l = {
    tytul  = { 4, 4, 240, 20 },
    x      = { pk.PALETA_SZER - 24, 4, 20, 20 },
    suwak  = { 4, 136, pk.PALETA_SUWAK_W, 20 },
    podglad = { 4, 160, pk.PALETA_SUWAK_W, 32 },
  }
  for i = 1, 24 do
    local kol = (i - 1) % 8
    local wiersz = math.floor((i - 1) / 8)
    l["probka_" .. i] = { 4 + kol * 36, 28 + wiersz * 36, 32, 32 }
  end
  return l
end

local function paleta_kolor_celu()
  if pk.paleta.cel == "ciemno" then return pk.cfg.kolor_ciemno end
  if pk.paleta.cel == "tekst" then return pk.cfg.kolor_tekstu end
  return pk.cfg.kolor_tla
end

-- tlo i ciemno maja kanal A (suwak); tekst jest zawsze kryjacy
local function paleta_ma_alfa()
  return pk.paleta.cel == "tlo" or pk.paleta.cel == "ciemno"
end

function pk.paleta_buduj()
  if pk.paleta_bg then return end
  local function mk(nazwa, r)
    return Geyser.Label:new({
      name = "pasek_kalendarz_paleta_" .. nazwa,
      x = r[1], y = r[2], width = r[3], height = r[4],
      fgColor = "white", message = "",
    })
  end
  local l = paleta_layout()
  pk.paleta_bg = mk("bg", { 0, 0, pk.PALETA_SZER, pk.PALETA_WYS })
  pk.paleta_bg:setColor(20, 20, 26, 255)
  pk.paleta_tytul = mk("tytul", l.tytul)
  pk.paleta_x = mk("x", l.x)
  pk.paleta_x:setColor(60, 60, 70, 255)
  pk.paleta_x:echo("&lt;center&gt;X")
  pk.paleta_x:setClickCallback(function() pk.paleta_zamknij() end)
  pk.paleta_probki = {}
  for i = 1, 24 do
    local p = mk("probka_" .. i, l["probka_" .. i])
    local c = pk.PALETA_KOLORY[i]
    p:setColor(c[1], c[2], c[3], 255)
    p:setClickCallback(function() pk.paleta_klik_probka(i) end)
    pk.paleta_probki[i] = p
  end
  pk.paleta_suwak = mk("suwak", l.suwak)
  pk.paleta_suwak:setClickCallback(function(ev) pk.paleta_klik_suwak(ev) end)
  pk.paleta_podglad = mk("podglad", l.podglad)
end

-- 1.6.12: kontrastowy kolor tekstu podgladu (bialy tekst na jasnym
-- wybranym kolorze byl niewidoczny). Blend nad tlem panelu (20,20,26)
-- przy alfa &lt; 255, potem luminancja Rec.601; prog 140. Arytmetyka
-- calkowita, deterministyczna.
function pk.paleta_kontrast(r, g, b, a)
  local function eff(c, bg) return math.floor((c * a + bg * (255 - a) + 127) / 255) end
  local er, eg, eb = eff(r, 20), eff(g, 20), eff(b, 26)
  local lum = (299 * er + 587 * eg + 114 * eb) / 1000
  if lum &gt; 140 then return 20, 20, 26 end
  return 255, 255, 255
end

-- odswieza tytul, gradient suwaka i podglad zgodnie z celem i alfa
function pk.paleta_odswiez()
  local k = paleta_kolor_celu()
  local r, g, b = k[1], k[2], k[3]
  local tytuly = {
    tlo = "Kolor: tlo paska",
    ciemno = "Kolor: pigulka CIEMNO",
    tekst = "Kolor: tekst paska",
  }
  pk.paleta_tytul:echo(tytuly[pk.paleta.cel] or "Kolor")
  if paleta_ma_alfa() then
    if pk.paleta_suwak.setStyleSheet then
      pk.paleta_suwak:setStyleSheet(string.format(
        "background-color: qlineargradient(x1:0, y1:0, x2:1, y2:0, stop:0 rgba(%d,%d,%d,0), stop:1 rgba(%d,%d,%d,255));",
        r, g, b, r, g, b))
    end
    pk.paleta_podglad:setColor(r, g, b, pk.paleta.alfa)
    pk.paleta_podglad:echo(string.format("&lt;center&gt;%d %d %d %d", r, g, b, pk.paleta.alfa))
  else
    pk.paleta_podglad:setColor(r, g, b, 255)
    pk.paleta_podglad:echo(string.format("&lt;center&gt;%d %d %d", r, g, b))
  end
  -- 1.6.12: kontrastowy kolor kodow liczbowych (wzorzec jak kolor_tekstu
  -- glownego labela: setFgColor przed kolejnym echem)
  setFgColor(pk.paleta_podglad.name, pk.paleta_kontrast(r, g, b,
    paleta_ma_alfa() and pk.paleta.alfa or 255))
end

-- pokazuje/ukrywa wszystkie widgety panelu na pozycji liczonej przy otwarciu
function pk.paleta_pokaz(pokaz)
  local py = pk.cfg.y + pk.cfg.height + 8
  if pk.stan.ciemno then py = py + pk.cfg.ciemno_odstep + pk.cfg.ciemno_height end
  local l = paleta_layout()
  -- cel bez kanalu A (tekst): suwak chowany, podglad na jego miejscu,
  -- panel nizszy o wysokosc suwaka z odstepem
  local alfa = paleta_ma_alfa()
  local bg_h = alfa and pk.PALETA_WYS or (pk.PALETA_WYS - l.suwak[4] - 4)
  local function ustaw(w, r)
    w:move(pk.cfg.x + r[1], py + r[2])
    if pokaz then w:show() else w:hide() end
  end
  ustaw(pk.paleta_bg, { 0, 0, pk.PALETA_SZER, bg_h })
  pk.paleta_bg:resize(pk.PALETA_SZER, bg_h)
  ustaw(pk.paleta_tytul, l.tytul)
  ustaw(pk.paleta_x, l.x)
  for i = 1, 24 do ustaw(pk.paleta_probki[i], l["probka_" .. i]) end
  ustaw(pk.paleta_suwak, l.suwak)
  if not alfa then pk.paleta_suwak:hide() end
  ustaw(pk.paleta_podglad, alfa and l.podglad
    or { l.podglad[1], l.suwak[2], l.podglad[3], l.podglad[4] })
end

function pk.paleta_otworz(cel)
  pk.paleta_buduj()
  pk.paleta.cel = cel
  pk.paleta.otwarta = true
  pk.paleta.alfa = paleta_ma_alfa() and (paleta_kolor_celu()[4] or 255) or 255
  pk.paleta_odswiez()
  pk.paleta_pokaz(true)
end

function pk.paleta_zamknij()
  if not pk.paleta.otwarta then return end
  pk.paleta.otwarta = false
  pk.paleta.cel = nil
  pk.paleta_pokaz(false)
end

-- cicha aplikacja: cfg + persystencja + przemalowanie paska, bez pk.say
function pk.paleta_zastosuj()
  pk.zapisz_kotwice()
  pk.zastosuj_wyglad()
end

function pk.paleta_klik_probka(i)
  local c = pk.PALETA_KOLORY[i]
  if not c then return end
  if pk.paleta.cel == "tekst" then
    pk.cfg.kolor_tekstu = { c[1], c[2], c[3] }
  else
    local k = { c[1], c[2], c[3], pk.paleta.alfa }
    if pk.paleta.cel == "ciemno" then pk.cfg.kolor_ciemno = k else pk.cfg.kolor_tla = k end
  end
  pk.paleta_odswiez()
  pk.paleta_zastosuj()
end

function pk.paleta_klik_suwak(ev)
  if not paleta_ma_alfa() then return end  -- tekst nie ma kanalu A
  local x = tonumber(ev and ev.x) or 0
  local a = math.floor(x / pk.PALETA_SUWAK_W * 255 + 0.5)
  if a &lt; 0 then a = 0 elseif a &gt; 255 then a = 255 end
  pk.paleta.alfa = a
  paleta_kolor_celu()[4] = a
  pk.paleta_odswiez()
  pk.paleta_zastosuj()
end

-- gole /pasek tlo i /pasek ciemno: toggle panelu / przelaczenie celu
function pk.komenda_paleta(cel)
  if pk.paleta.otwarta and pk.paleta.cel == cel then
    pk.paleta_zamknij()
    return
  end
  pk.paleta_otworz(cel)
end

function pk.komenda_pomoc()
  printTitledReport({
    "Pasek kalendarza  (v" .. PLUGIN_VERSION .. " | " .. PLUGIN_BUILD .. ")",
    "",
    "Komendy:",
    "  /pasek                        - synchronizacja zegara (wysyla czas; to samo robi klikniecie paska)",
    "  /pasek on | off               - pokazuje / chowa pasek (ustawienia zostaja, nie sa kasowane)",
    "  /pasek float                  - przenoszenie mysza na 60 s; w trybie okna: powrot do glownego okna",
    "  /pasek float on | off         - wlacza (auto-blokada po 60 s bez ruchu) / wylacza przenoszenie mysza",
    "  /pasek window                 - przelacznik: osobne okno &lt;-&gt; glowne okno jako pasek",
    "  /pasek window on | off        - pasek w osobnym oknie / powrot do glownego okna jako pasek",
    "  /pasek pozycja X Y            - przesuniecie paska (pigulka CIEMNO podaza za paskiem)",
    "  /pasek wartosci               - aktualne wartosci paska (domyslne w nawiasie)",
    "  /pasek tlo R G B [A]          - tlo paska; bez argumentow: paleta kolorow i przezroczystosci do wyboru",
    "  /pasek tekst R G B            - kolor tekstu paska; bez argumentow: paleta kolorow do wyboru",
    "  /pasek ciemno R G B [A]       - tlo pigulki CIEMNO; bez argumentow: paleta kolorow i przezroczystosci do wyboru",
    "  /pasek domyslne               - powrot do ustawien domyslnych",
    "  /pasek ustaw imperium 8 273   - reczne ustawienie zegara (domena, godzina 0-23, dzien roku 1-400)",
    "  /pasek ustaw ishtar 18 90     - to samo dla domeny Ishtar (dzien roku 1-360)",
    "  /pasek reset                  - czysci zapisane kotwice czasu dla obu domen",
    "  /pasek reset [domena]         - czysci kotwice czasu tylko podanej domeny (ishtar | imperium)",
    "  /pasek aktualizuj             - sprawdz i zainstaluj aktualizacje",
    "  Kolory: R G B [A], kazda wartosc to liczba 0-255.",
    "  R G B = kanaly czerwony, zielony, niebieski. A = przezroczystosc tla:",
    "  0 = niewidzialne, 255 = calkowicie kryjace (im mniej, tym bardziej przezroczyste).",
    "  Bez A: tlo paska dostaje fabryczne 210, pigulka CIEMNO 230.",
    "  Samo /pasek tlo, /pasek ciemno lub /pasek tekst otwiera palete kolorow.",
    "  Paleta: 24 kolory; tlo i ciemno maja suwak przezroczystosci A, tekst nie.",
    "  Przyklady:",
    "    /pasek tlo 0 0 0 255     - czarne tlo, calkowicie kryjace",
    "    /pasek tlo 0 0 0 120     - czarne tlo, polprzezroczyste",
    "    /pasek tlo 0 0 0 0       - tlo niewidzialne, zostaje sam tekst",
    "    /pasek tekst 255 255 0   - zolty tekst",
    "  Ustawienia zapisuja sie na dysku i przechodza przez restart klienta.",
  }, "\n")
end

-- ---------- GMCP ----------
function pk.gmcp_update()
  -- domena z GMCP (jak w kliencie Dargoth: gmcp.room.info.map.domain)
  if gmcp and gmcp.room and gmcp.room.info and gmcp.room.info.map
     and gmcp.room.info.map.domain then
    local d = gmcp.room.info.map.domain
    if d == "Imperium" then pk.stan.domena = "imperium"
    elseif d == "Ishtar" then pk.stan.domena = "ishtar" end
  end

  if gmcp and gmcp.room and gmcp.room.time then
    local t = gmcp.room.time
    if t.daylight ~= nil then
      local dl = t.daylight and true or false
      if pk.stan.daylight ~= nil and pk.stan.daylight ~= dl then
        -- zmiana dzien/noc = zaobserwowany wschod/zachod: dokrec zegar
        pk.rekalibruj_na_przejsciu(dl and "wschod" or "zachod")
      end
      pk.stan.daylight = dl
    end
    if t.season ~= nil then pk.stan.season = t.season end
  end

  if gmcp and gmcp.objects and gmcp.objects.data then
    local ciemno = false
    for _, v in pairs(gmcp.objects.data) do
      if type(v) == "table" and v.can_see_in_room == false then ciemno = true break end
    end
    pk.stan.ciemno = ciemno
  end

  pk.render()
end

-- ---------- RENDER ----------
function pk.render()
  if not pk.label then return end
  local domena, doy, hh, mm, precision = pk.aktualny_czas()

  local dl = pk.stan.daylight
  if dl == nil and domena then dl = pk.jest_jasno(domena, doy, hh) end

  local ikona = (dl == true) and "☀" or (dl == false) and "☾" or "◌"
  local pora = (dl == true) and "DZIEŃ" or (dl == false) and "NOC" or "--"
  local czas = hh and string.format("%02d:%02d", hh, mm) or "--:--"
  if hh and pk.cfg.pokazuj_precyzje and precision and precision &gt; 0 and precision &lt; 60 then
    czas = czas .. " ±" .. precision
  end
  local dom = (domena == "ishtar") and "ISHTAR" or (domena == "imperium") and "IMPERIUM"
  if not dom then
    dom = (pk.stan.domena == "ishtar") and "ISHTAR" or (pk.stan.domena == "imperium") and "IMPERIUM" or "..."
  end

  local l1 = ikona .. " " .. pora .. " " .. czas .. " | " .. dom

  local l2
  if domena == "ishtar" then
    local nazwa, dzien = pk.ishtar_pora_dnia(doy)
    l2 = string.format("DZIEŃ %d | %s | DZIEŃ ROKU %d/360", dzien, nazwa, doy)
  elseif domena == "imperium" then
    local nazwa, dzien = pk.imp_miesiac_dnia(doy)
    l2 = string.format("DZIEŃ %d | %s | DZIEŃ ROKU %d/400", dzien, nazwa, doy)
  else
    l2 = "wpisz czas albo kliknij pasek"
  end

  pk.label:echo("&lt;center&gt;" .. l1 .. "&lt;br&gt;" .. l2)

  -- pigulka CIEMNO pod paskiem (osobny label, dokladnie na srodku)
  if pk.ciemno_label then
    if pk.stan.ciemno and pk.cfg.wlaczony ~= false then pk.ciemno_label:show() else pk.ciemno_label:hide() end
  end

  -- dynamiczny resize osobnego okna przy zmianie CIEMNO (tylko przy zmianie)
  pk.window_resize()
end

-- ---------- KOMENDA WARTOSCI (1.6.0) ----------
local function fmt_rgb(k)
  return table.concat(k, " ")
end

function pk.komenda_wartosci()
  local c, f = pk.cfg, pk.FABRYCZNE
  printTitledReport({
    "Pasek kalendarza  (v" .. PLUGIN_VERSION .. " | " .. PLUGIN_BUILD .. ")",
    "Aktualne wartosci (w nawiasach fabryczne):",
    string.format("  pozycja: x=%d y=%d  (x=%d y=%d)", c.x, c.y, f.x, f.y),
    "  widocznosc: " .. (c.wlaczony ~= false and "wlaczony" or "wylaczony") .. "  (wlaczony)",
    "  umiejscowienie: " .. (c.window == true and "osobne okno" or "glowne okno") ..
      " | float: " .. (pk.float_on == true and "wlaczony" or "wylaczony") .. "  (glowne okno)",
    "  tlo:     " .. fmt_rgb(c.kolor_tla) .. "  (" .. fmt_rgb(f.kolor_tla) .. ")",
    "  tekst:   " .. fmt_rgb(c.kolor_tekstu) .. "  (" .. fmt_rgb(f.kolor_tekstu) .. ")",
    "  ciemno:  " .. fmt_rgb(c.kolor_ciemno) .. "  (" .. fmt_rgb(f.kolor_ciemno) .. ")",
  }, "\n\n")
end

-- ---------- AUTOAKTUALIZACJA (1.6.0) ----------
-- Repo publiczne na GitHub: releases/latest (anonimowe API) -&gt; asset
-- .mpackage -&gt; uninstallPackage -&gt; installPackage (wzorzec skryptow
-- aktualizacji Tjurczyka). Check przy starcie z throttlem 8 h (cache na
-- dysku profilu); miedzy checkami powiadomienie odtwarzane z knownLatest.
-- Komenda /pasek aktualizuj pomija throttle. Przy aktualizacji sprzata
-- tez stara nazwe paczki ("pasek kalendarz arkadia" - przed 1.6.0).
-- Stala tozsamosc paczki (F1/F2): download do &lt;pkg&gt;.mpackage i sweep
-- legacy nazw przed instalacja (mudlet-web nazywa paczke od pliku).
local UPDATE_REPO         = "arkadia-mudlet-pasek_czas"
local UPDATE_PKG_NAME     = "pasek_kalendarz_arkadia"
local UPDATE_LEGACY_PKG   = "pasek kalendarz arkadia"
local UPDATE_CMD          = "/pasek aktualizuj"
local UPDATE_THROTTLE_SEC = 8 * 3600
local UPDATE_API_URL      = "https://api.github.com/repos/Isithunzi000/" .. UPDATE_REPO .. "/releases/latest"
local UPDATE_CACHE_PATH   = getMudletHomeDir() .. "/pasek_kalendarz_update_cache.lua"
-- Historyczne nazwy paczek na mudlet-web (tozsamosc = nazwa pliku):
-- wersjonowane assety (baza pasek_kalendarz) + dawniej &lt;pkg&gt;_update.
-- Komplet wg Releases API (tagi 1.2.0-1.6.1 ciagle, bez dzur);
-- self = kamizelka na lokalne buildy.
local UPDATE_LEGACY_PKGS = {
  "pasek_kalendarz_update",
  "pasek_kalendarz_1_2_0", "pasek_kalendarz_1_3_0", "pasek_kalendarz_1_4_0",
  "pasek_kalendarz_1_4_1", "pasek_kalendarz_1_5_0", "pasek_kalendarz_1_5_1",
  "pasek_kalendarz_1_6_1",
  "pasek_kalendarz_" .. PLUGIN_VERSION:gsub("%.", "_"),
}

local updateDlHandlers = {}
local updateMetaPath = nil
local updatePkgPath = nil
local updatePendingVersion = nil
local updateManual = false

local function updateCacheRead()
  local t = {}
  local ok, res = pcall(table.load, UPDATE_CACHE_PATH, t)
  if ok and res then return t end
  return {}
end

local function updateCacheWrite(latest)
  pcall(table.save, UPDATE_CACHE_PATH, { lastCheck = os.time(), knownLatest = latest })
end

local function updateVersionParts(v)
  local parts = {}
  for seg in tostring(v):gmatch("[^%.]+") do
    parts[#parts + 1] = tonumber(seg:match("^%d+")) or 0
  end
  return parts
end

-- Porownanie numeryczne segmentow: 1.4.9 &lt; 1.5.1 (string dalby falszywy alarm).
local function updateVersionNewer(a, b)
  local pa, pb = updateVersionParts(a), updateVersionParts(b)
  for i = 1, math.max(#pa, #pb) do
    local x, y = pa[i] or 0, pb[i] or 0
    if x ~= y then return x &gt; y end
  end
  return false
end

local function updateSay(msg)
  pk.say(msg)
end

local function updateNotify(latest)
  safeCecho("\n" .. C_HEADER .. "[pasek_kalendarz] Dostepna nowa wersja " .. latest ..
    " (masz " .. PLUGIN_VERSION .. "). Wpisz " .. C_NOW .. UPDATE_CMD ..
    C_RESET .. C_HEADER .. " aby zaktualizowac." .. C_RESET .. "\n\n")
end

local function updateFail(what)
  updateSay("Nie udalo sie " .. what .. " - sprobuj pozniej.")
end

local function updateKillHandlers()
  for k, id in pairs(updateDlHandlers) do
    killAnonymousEventHandler(id)
    updateDlHandlers[k] = nil
  end
end

local function updateOnError(event)
  updateKillHandlers()
  updateMetaPath = nil
  updatePkgPath = nil
  updatePendingVersion = nil
  updateFail("sprawdzic aktualizacji")
end

local function updateOnPkgDone(event, path)
  if path ~= updatePkgPath then return end
  updateKillHandlers()
  local v = updatePendingVersion
  updatePkgPath = nil
  updatePendingVersion = nil
  pcall(uninstallPackage, UPDATE_LEGACY_PKG)
  pcall(uninstallPackage, UPDATE_PKG_NAME)
  for _, legacyName in ipairs(UPDATE_LEGACY_PKGS) do
    pcall(uninstallPackage, legacyName)
  end
  tempTimer(1, function()
    installPackage(path)
    local ok = os.remove(path)
    updateSay("Zaktualizowano do wersji " .. v .. ". Zrestartuj Mudleta, aby zmiany zadzialaly.")
  end)
end

local function updateOnMetaDone(event, path)
  if path ~= updateMetaPath then return end
  updateMetaPath = nil
  if updateDlHandlers.meta then
    killAnonymousEventHandler(updateDlHandlers.meta)
    updateDlHandlers.meta = nil
  end
  local ok, meta = pcall(function()
    local f = io.open(path, "r")
    if not f then error("brak pliku metadanych") end
    local body = f:read("*a")
    f:close()
    return yajl.to_value(body)
  end)
  if not ok or type(meta) ~= "table" or type(meta.tag_name) ~= "string" then
    updateKillHandlers()
    updateFail("sprawdzic aktualizacji")
    return
  end
  local latest = meta.tag_name:gsub("^v", "")
  updateCacheWrite(latest)
  if not updateVersionNewer(latest, PLUGIN_VERSION) then
    updateKillHandlers()
    if updateManual then updateSay("Masz najnowsza wersje (" .. PLUGIN_VERSION .. ").") end
    return
  end
  if not updateManual then
    updateKillHandlers()
    updateNotify(latest)
    return
  end
  local url = nil
  if type(meta.assets) == "table" then
    for _, a in ipairs(meta.assets) do
      if type(a) == "table" and type(a.browser_download_url) == "string"
         and a.browser_download_url:match("%.mpackage$") then
        url = a.browser_download_url
        break
      end
    end
  end
  if not url then
    url = "https://github.com/Isithunzi000/" .. UPDATE_REPO .. "/releases/download/v" ..
          latest .. "/" .. UPDATE_PKG_NAME .. ".mpackage"
  end
  updatePendingVersion = latest
  updatePkgPath = getMudletHomeDir() .. "/" .. UPDATE_PKG_NAME .. ".mpackage"
  updateDlHandlers.pkg = registerAnonymousEventHandler("sysDownloadDone", updateOnPkgDone)
  downloadFile(updatePkgPath, url)
end

local function startUpdateCheck(manual)
  updateManual = manual and true or false
  if mudletOlderThan and mudletOlderThan(4, 6) then
    if updateManual then updateSay("Aktualizator wymaga Mudleta 4.6 lub nowszego.") end
    return
  end
  if not updateManual then
    local cache = updateCacheRead()
    local lastCheck = tonumber(cache.lastCheck) or 0
    if os.time() - lastCheck &lt; UPDATE_THROTTLE_SEC then
      if type(cache.knownLatest) == "string"
         and updateVersionNewer(cache.knownLatest, PLUGIN_VERSION) then
        updateNotify(cache.knownLatest)
      end
      return
    end
  end
  updateMetaPath = getMudletHomeDir() .. "/pasek_kalendarz_update_meta.json"
  updateDlHandlers.meta = registerAnonymousEventHandler("sysDownloadDone", updateOnMetaDone)
  updateDlHandlers.err  = registerAnonymousEventHandler("sysDownloadError", updateOnError)
  downloadFile(updateMetaPath, UPDATE_API_URL)
end

function pk.onAktualizuj()
  startUpdateCheck(true)
end

-- check przy starcie klienta (opozniony, zeby nie blokowac ladowania)
tempTimer(10, function() startUpdateCheck(false) end)

function pk.tick()
  pk.render()
end

-- komunikat ladowania (paritet z ishtar/imperium)
if cecho then
  cecho("&lt;green&gt;[pasek_kalendarz]&lt;reset&gt; Pasek kalendarza v" .. PLUGIN_VERSION ..
        " zaladowany. Wpisz &lt;yellow&gt;/pasek&lt;reset&gt; (pomoc: &lt;yellow&gt;/pasek pomoc&lt;reset&gt;).\n")
end</script>
				<eventHandlerList>
				</eventHandlerList>
			</Script>
			<Script isActive="yes" isFolder="no">
				<name>ui</name>
				<packageName></packageName>
				<script>-- tworzy pasek (uruchamiane przy ladowaniu paczki/profilu)
local pk = pasek_kalendarz

-- najpierw zapisana konfiguracja (kotwice + wyglad z komend "/pasek ...")
pk.wczytaj_kotwice()

-- budowa labeli: zawsze inline; przy persystowanej fladze window etykiety
-- przepinane do osobnego okna przez setWindow (/pasek window)
pk.buduj_labele()
if pk.cfg.window == true and pk.window_dostepne() then pk.window_wejdz() end</script>
				<eventHandlerList>
				</eventHandlerList>
			</Script>
			<Script isActive="yes" isFolder="no">
				<name>pasek_kalendarz_gmcp</name>
				<packageName></packageName>
				<script>function pasek_kalendarz_gmcp()
  pasek_kalendarz.gmcp_update()
end</script>
				<eventHandlerList>
					<string>gmcp.room.time</string>
					<string>gmcp.room.info</string>
					<string>gmcp.objects.data</string>
				</eventHandlerList>
			</Script>
		</ScriptGroup>
	</ScriptPackage>
	<TriggerPackage>
		<TriggerGroup isActive="yes" isFolder="yes">
			<name>pasek_kalendarz</name>
			<packageName></packageName>
			<script></script>
			<triggerType>0</triggerType>
			<conditonLineDelta>0</conditonLineDelta>
			<mStayOpen>0</mStayOpen>
			<mCommand></mCommand>
			<mFgColor>#ff0000</mFgColor>
			<mBgColor>#ffff00</mBgColor>
			<mSoundFile></mSoundFile>
			<colorTriggerFgColor>#000000</colorTriggerFgColor>
			<colorTriggerBgColor>#000000</colorTriggerBgColor>
			<regexCodeList />
			<regexCodePropertyList />
			<Trigger isActive="yes" isFolder="no" isTempTrigger="no" isMultiline="no" isPerlSlashGOption="no" isColorizerTrigger="no" isFilterTrigger="no" isSoundTrigger="no" isColorTrigger="no" isColorTriggerFg="no" isColorTriggerBg="no">
				<name>czas_line</name>
				<script>pasek_kalendarz.on_czas_line(matches[1])</script>
				<triggerType>0</triggerType>
				<conditonLineDelta>0</conditonLineDelta>
				<mStayOpen>0</mStayOpen>
				<mCommand></mCommand>
				<packageName></packageName>
				<mFgColor>#ff0000</mFgColor>
				<mBgColor>#ffff00</mBgColor>
				<mSoundFile></mSoundFile>
				<colorTriggerFgColor>#000000</colorTriggerFgColor>
				<colorTriggerBgColor>#000000</colorTriggerBgColor>
				<regexCodeList>
					<string>^(Jest w przyblizeniu .*)$</string>
				</regexCodeList>
				<regexCodePropertyList>
					<integer>1</integer>
				</regexCodePropertyList>
			</Trigger>
		</TriggerGroup>
	</TriggerPackage>
	<TimerPackage>
		<TimerGroup isActive="yes" isFolder="yes">
			<name>pasek_kalendarz</name>
			<packageName></packageName>
			<script></script>
			<command></command>
			<time>00:00:00.000</time>
			<Timer isActive="yes" isFolder="no" isTempTimer="no" isOffsetTimer="no">
				<name>tick</name>
				<script>if pasek_kalendarz and pasek_kalendarz.tick then pasek_kalendarz.tick() else killTimer("tick") end</script>
				<command></command>
				<packageName></packageName>
				<time>00:00:02.000</time>
			</Timer>
		</TimerGroup>
	</TimerPackage>
	<AliasPackage>
		<AliasGroup isActive="yes" isFolder="yes">
			<name>pasek_kalendarz</name>
			<packageName></packageName>
			<script></script>
			<command></command>
			<regex></regex>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_sync</name>
				<script>send("czas")</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_reset</name>
				<script>pasek_kalendarz.komenda_reset()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek reset$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_reset_domena</name>
				<script>pasek_kalendarz.komenda_reset(matches[2])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek reset (.+)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_ustaw</name>
				<script>pasek_kalendarz.ustaw_recznie(matches[2], matches[3], matches[4])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek ustaw (imperium|ishtar) (\d+)(?: (\d+))?$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_on</name>
				<script>pasek_kalendarz.komenda_on()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek on$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_off</name>
				<script>pasek_kalendarz.komenda_off()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek off$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_float</name>
				<script>pasek_kalendarz.komenda_float()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek float$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_float_arg</name>
				<script>pasek_kalendarz.komenda_float(matches[2])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek float (.+)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_window</name>
				<script>pasek_kalendarz.komenda_window()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek window$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_window_arg</name>
				<script>pasek_kalendarz.komenda_window(matches[2])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek window (.+)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_pomoc</name>
				<script>pasek_kalendarz.komenda_pomoc()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek (pomoc|help)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_pozycja</name>
				<script>pasek_kalendarz.komenda_pozycja(matches[2], matches[3])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek pozycja (\d+) (\d+)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_tlo</name>
				<script>pasek_kalendarz.komenda_tlo(matches[2], matches[3], matches[4], matches[5])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek tlo (\d+) (\d+) (\d+)(?: (\d+))?$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_tekst</name>
				<script>pasek_kalendarz.komenda_tekst(matches[2], matches[3], matches[4])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek tekst (\d+) (\d+) (\d+)$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_ciemno</name>
				<script>pasek_kalendarz.komenda_ciemno(matches[2], matches[3], matches[4], matches[5])</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek ciemno (\d+) (\d+) (\d+)(?: (\d+))?$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_tlo_paleta</name>
				<script>pasek_kalendarz.komenda_paleta("tlo")</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek tlo$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_ciemno_paleta</name>
				<script>pasek_kalendarz.komenda_paleta("ciemno")</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek ciemno$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_tekst_paleta</name>
				<script>pasek_kalendarz.komenda_paleta("tekst")</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek tekst$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_domyslne</name>
				<script>pasek_kalendarz.komenda_domyslne()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek domyslne$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_wartosci</name>
				<script>pasek_kalendarz.komenda_wartosci()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek wartosci$</regex>
			</Alias>
			<Alias isActive="yes" isFolder="no">
				<name>pasek_aktualizuj</name>
				<script>pasek_kalendarz.onAktualizuj()</script>
				<command></command>
				<packageName></packageName>
				<regex>^/pasek aktualizuj$</regex>
			</Alias>
		</AliasGroup>
	</AliasPackage>
</MudletPackage>
